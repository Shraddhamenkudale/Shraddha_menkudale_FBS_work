#include <stdio.h>

int main()
{
    float base, height, area;

    base = 11.3;
    height = 5.4;

    area = base * height;

    printf("Base = %f\n", base);
    printf("Height = %f\n", height);
    printf("Area = %f", area);

    return 0;
}